program data(input, output, diskFile);

uses wincrt;


const fileName = 'file.dat';
var diskFile : text;

procedure writeFile(diskFile : text);
	begin
	
	end;
procedure readFile(diskFile : text);
	begin
	
	end;
procedure printFile(diskFile : text);{Possibly won't need this}
	begin
	
	end;
begin
	
end.

{Data Files}
{Kaleb Haslam}